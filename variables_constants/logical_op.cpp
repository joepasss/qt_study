// Comparison & logical operators
// compares different variables
// x == y, x < y, x <= y, x != y, etc ...

void logical() {
  int a = 10;
  int b = 12;

  qInfo() << "" << (a == b);
  qInfo() << "" << (a < b);
}